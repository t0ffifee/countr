import 'package:flutter/material.dart';

// Dit moet een bestand worden die de gehele kaart kan maken
// stop hier alle classes in die de kaart gebruikt
// (we beginnen met het maken van een static kaart zonder countdown)

class Card extends StatelessWidget {
  const Card({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
